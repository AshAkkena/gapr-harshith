<h1>Active Users</h1>
This is a list of all regular users who have signed up, been activated, and have association with a provider in some way.

<table class="apa">
  <thead>
    <tr>
      <th>User</th>
      <th>Site Admin</th>
      <th>Facilitator</th>
    </tr>
  </thead>

  <tbody>
    <% @active_users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td>
        <% if user.has_role? :site_admin, :any %>
          <% Provider.with_role(:site_admin, user).each do |provider| %>
            <div><%= provider.period.to_s + ": " + provider.long_name %>
          <% end %>
        <% else %>
          <div>(None)</div>
        <% end %>
        </td>
        <td>
        <% if user.has_role? :facilitator, :any %>
          <% Provider.with_role(:facilitator, user).each do |provider| %>
            <div><%= provider.period.to_s + ": " + provider.long_name %>
          <% end %>
        <% else %>
          <div>(None)</div>
        <% end %>
        </td>

      </tr>
    <% end %>
  </tbody>
</table>

<h1>Ghost Users</h1>
These users are active, but they do not have any provider associated with them.
<ul>
  <% if @ghost_users.length > 0 %>
    <% @ghost_users.each do |user| %>
        <li><%= user.email %></li>
    <% end %>
  <% else %>
    <li>(None)</li>
  <% end %>
</ul>

<h1>Inactive Users</h1>
These users have not been activated. They could be anyone, from anywhere. They cannot get in.
<ul>
  <% if @inactive_users.length > 0 %>
    <% @inactive_users.each do |user| %>
        <li><%= user.email %></li>
    <% end %>
  <% else %>
    <li>(None)</li>
  <% end %>
</ul>
